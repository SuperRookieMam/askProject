package ~[packagedao];

import com.ask.base.dao.BaseRepository;
import ~[entityname];
import org.springframework.stereotype.Repository;

@Repository
public interface ~[entitySimpleName]Repository extends BaseRepository<~[entitySimpleName],~[ID]> {
}
