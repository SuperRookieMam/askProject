package ~[packageServiceImpl];

import com.ask.base.service.impl.BaseServiceImpl
import ~[entityname];
import ~[parantService];
import org.springframework.stereotype.Service;

@Service
public class ~[entitySimpleName]ServiceImpl extends BaseServiceImpl<~[entitySimpleName], ~[ID]> implements ~[entitySimpleName]Service {
}
